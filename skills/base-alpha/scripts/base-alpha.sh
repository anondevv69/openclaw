#!/usr/bin/env bash
# Base Alpha has no single script — the agent runs the workflow via web_fetch (Zapper, Neynar, InfoBot) and exec (bankr.sh for trades only, neynar.sh post for casts). See skills/base-alpha/SKILL.md.
echo "Base Alpha: no single script. Follow SKILL.md: use web_fetch for Zapper/Neynar/InfoBot, exec bankr.sh only for trades, exec neynar.sh post for Farcaster casts."
